<section class="portfolio-hero">
    <div class="portfolio-hero__glow portfolio-hero__glow--left"></div>
    <div class="portfolio-hero__glow portfolio-hero__glow--right"></div>

    <div class="portfolio-hero__content">
        <p class="portfolio-hero__eyebrow">
            Hi, I’m Diego 👋
        </p>

        <h1 class="portfolio-hero__title">
            Building software
            <span>that solves real problems.</span>
        </h1>

        <p class="portfolio-hero__text">
            I’m a junior software developer focused on building web
            applications that simplify processes, improve productivity,
            and deliver useful digital experiences.
        </p>

        <div class="portfolio-hero__buttons">
            <a
                href="#projects"
                class="portfolio-hero__button portfolio-hero__button--primary"
            >
                View Projects
                <span aria-hidden="true">&lt;/&gt;</span>
            </a>

            <a
                href="#about"
                class="portfolio-hero__button portfolio-hero__button--secondary"
            >
                About Me
                <span aria-hidden="true">→</span>
            </a>
        </div>

        <div class="portfolio-hero__status">
            <span class="portfolio-hero__status-dot"></span>

            <span>
                Junior Software Developer · Based in Colombia
            </span>
        </div>
    </div>
</section>

<style>
    .portfolio-hero {
        position: relative;
        display: flex;
        align-items: center;

        min-height: 100vh;
        overflow: hidden;

        padding: 8rem 1rem 5rem;
        background: transparent;
    }

    .portfolio-hero__content {
        position: relative;
        z-index: 2;

        width: 100%;
        max-width: 72rem;
        margin: 0 auto;
    }

    .portfolio-hero__eyebrow {
        margin: 0 0 1.25rem;

        font-size: 0.8rem;
        font-weight: 600;
        letter-spacing: 0.3em;
        text-transform: uppercase;

        background: linear-gradient(
            90deg,
            #b5d2fd,
            #7c60fa
        );

        background-clip: text;
        -webkit-background-clip: text;

        color: transparent;
        -webkit-text-fill-color: transparent;
    }

    .portfolio-hero__title {
        max-width: 900px;
        margin: 0;

        font-size: clamp(3rem, 7vw, 5.8rem);
        font-weight: 800;
        line-height: 0.98;
        letter-spacing: -0.055em;

        color: rgb(var(--rgb-text-primary));
    }

    .portfolio-hero__title span {
        display: block;

        background: linear-gradient(
            90deg,
            #a78bfa 0%,
            #8b5cf6 30%,
            #4f46e5 55%,
            #2563eb 78%,
            #60a5fa 100%
        );

        background-clip: text;
        -webkit-background-clip: text;

        color: transparent;
        -webkit-text-fill-color: transparent;
    }

    .portfolio-hero__text {
        max-width: 680px;
        margin: 1.75rem 0 0;

        font-size: clamp(1rem, 2vw, 1.2rem);
        font-weight: 400;
        line-height: 1.8;

        color: rgb(var(--rgb-text-secondary));
    }

    .portfolio-hero__buttons {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;

        margin-top: 2rem;
    }

    .portfolio-hero__button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.65rem;

        min-height: 52px;
        padding: 0.85rem 1.4rem;

        border-radius: 0.75rem;

        font-size: 0.95rem;
        font-weight: 600;
        text-decoration: none;

        transition:
            transform 0.25s ease,
            border-color 0.25s ease,
            box-shadow 0.25s ease,
            background-color 0.25s ease;
    }

    .portfolio-hero__button--primary {
        color: rgb(var(--rgb-text-on-accent));
        border: 1px solid rgb(var(--rgb-accent-purple) / 0.35);

        background: linear-gradient(
            90deg,
            #6d28d9,
            #4f46e5,
            #2563eb
        );

        box-shadow:
            0 10px 30px rgba(79, 70, 229, 0.22),
            0 0 25px rgb(var(--rgb-accent-purple-strong) / 0.12);
    }

    .portfolio-hero__button--primary:hover {
        transform: translateY(-3px);

        box-shadow:
            0 16px 40px rgba(79, 70, 229, 0.3),
            0 0 35px rgb(var(--rgb-accent-purple-strong) / 0.18);
    }

    .portfolio-hero__button--secondary {
        color: rgb(var(--rgb-text-nav));
        border: 1px solid rgb(var(--rgb-border-gray) / 0.2);
        background: rgb(var(--rgb-bg-base) / 0.65);
    }

    .portfolio-hero__button--secondary:hover {
        transform: translateY(-3px);
        border-color: rgb(var(--rgb-accent-purple) / 0.4);
        background: rgb(var(--rgb-bg-elevated) / 0.85);
    }

    .portfolio-hero__status {
        display: flex;
        align-items: center;
        gap: 0.65rem;

        margin-top: 2rem;

        font-size: 0.85rem;
        color: rgb(var(--rgb-text-muted));
    }

    .portfolio-hero__status-dot {
        width: 0.55rem;
        height: 0.55rem;

        flex-shrink: 0;

        border-radius: 9999px;
        background: rgb(var(--rgb-success));

        box-shadow: 0 0 14px rgb(var(--rgb-success) / 0.6);
    }

    .portfolio-hero__glow {
        position: absolute;
        pointer-events: none;

        border-radius: 9999px;
        filter: blur(90px);
    }

    .portfolio-hero__glow--left {
        top: 15%;
        left: -12%;

        width: 30rem;
        height: 30rem;

        background: rgb(var(--rgb-accent-purple-deep) / var(--opacity-glow-hero-1));
    }

    .portfolio-hero__glow--right {
        right: -10%;
        bottom: 8%;

        width: 26rem;
        height: 26rem;

        background: rgb(var(--rgb-accent-blue-deep) / var(--opacity-glow-hero-2));
    }

    @media (max-width: 640px) {
        .portfolio-hero {
            min-height: auto;
            padding-top: 9rem;
            padding-bottom: 6rem;
        }

        .portfolio-hero__title {
            line-height: 1.05;
        }

        .portfolio-hero__buttons {
            flex-direction: column;
            align-items: stretch;
        }

        .portfolio-hero__button {
            width: 100%;
        }

        .portfolio-hero__status {
            align-items: flex-start;
        }
    }
</style>