<div class="hero">
    <div class="hero-content">
        <p class="hero-intro"><span class="hero-intro-span">Hola, soy Diego</span>👋</p>

        <h1 class="hero-description">
            Building software <br>
            and <span class="span-gradient-two">digital experiences</span>
        </h1>

        <p>
            Building software that simplifies processes
            and improves productivity.
        </p>

        <div class="hero-button-container">
            <button class="hero-button hero-button-a hero-button-projects">
                View Projects &lt;/&gt;
            </button>
            <button class="hero-button hero-button-b">
                About me
            </button>
        </div>

    </div>
</div>

<style>
    .hero {
        position: relative;
        top: 20rem;
        margin-left: 4rem;
    }

    .hero-content {
        max-width: 700px;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: 20px;
    }

    .hero-button-container {
        min-width: 250px;
        display: flex;
        align-items: center;
        gap: 1rem;
    }

    .hero-intro {
        font-size: 2rem;
    }

    .hero-intro-span {
        background: linear-gradient(
            90deg,
            #7c3aed,
            #2563eb
        );

        background-clip: text;
        -webkit-background-clip: text;

        color: transparent;
        -webkit-text-fill-color: transparent;

    }

    .hero-description {
        font-size: 3.5rem;
        font-weight: 700;
    }

    .span-gradient-two {
        background: linear-gradient(
            90deg,
            #7c3aed 0%,
            #8b5cf6 20%,
            #4f46e5 40%,
            #2563eb 70%,
            #60a5fa 100%
        );

        background-clip: text;
        -webkit-background-clip: text;

        color: transparent;
        -webkit-text-fill-color: transparent;
    }

    .hero-button {
        padding: 1rem 1.5rem;
        font-size: 1.2rem;
        color: #cbd5e1;
        background: #0b0d1c;

        border: none;
        border-radius: 10px;
    }

    .hero-button-b {
        border: 1px solid #2e2e2e;
    }

    .hero-button-projects {
        background: linear-gradient(
            90deg,
            #472be6,
            #2563eb
        );
    }

    .hero
</style>
